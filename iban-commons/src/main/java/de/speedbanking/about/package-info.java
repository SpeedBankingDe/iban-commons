/**
 * Contains the library's own metadata entry point.
 *
 * <h2>Key classes</h2>
 * <dl>
 *   <dt>{@link de.speedbanking.about.ThisLib}</dt>
 *   <dd>Manifest-driven {@code main()} entry point that prints title, version, vendor and build
 *     information when this JAR is executed directly instead of being used as a dependency.</dd>
 * </dl>
 *
 * @since 1.8.11
 */
package de.speedbanking.about;
